import ContactForm from "./ContactForm";
import Section from "../common/Section";

function Contact() {
  return (
    <Section id="contact" title="Contacto" aos="fade-left">
      <ContactForm />
    </Section>
  );
}

export default Contact;
